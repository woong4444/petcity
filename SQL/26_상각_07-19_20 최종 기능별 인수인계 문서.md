# 상각_07-19_20 최종 기능별 인수인계 문서

## 1. 비교 기준 및 보호 원칙

- 팀장 원본(읽기 비교만 수행): `C:\Users\user\OneDrive - gjcu.ac.kr\바탕 화면\07-19\petcity_test`
- 작업본(8080): `D:\petcity`
- 비교 제외: `.git`, `.gradle`, `build`, `out`, `.idea`
- 8081 팀장 원본은 추가·수정·삭제하지 않았습니다.
- 팀장 원본과 공통 경로인 Java, Controller, DTO, DAO, Service, Mapper, HTML 파일의 내용 변경은 없습니다.
- 기능은 8080의 독립 패키지·필터·템플릿·매퍼·SQL로 추가했습니다.

## 2. 최종 비교 결과

- 8080에만 존재하는 추가 파일: 이 문서를 포함해 46개
- 공통 경로이지만 내용이 다른 파일: `src/main/resources/application.yml` 1개
- 8081에만 있고 8080에서 삭제된 파일: 없음
- `application.yml` 차이는 8080 로컬 실행용 DB/OAuth/메일 설정이며 Git 공유 대상에서 제외합니다.

## 3. 기능별 추가 파일

### 3.1 회원 탈퇴·복구 핵심 기능

- `src/main/java/com/jjang051/petcity/memberfeature/config/MemberFeatureSecurityConfig.java`
  - 독립 회원 기능 URL과 로그인 실패 처리 연결
- `src/main/java/com/jjang051/petcity/memberfeature/controller/MemberFeatureController.java`
  - 탈퇴, 복구, 마이페이지, 아이디 찾기, 비밀번호 재설정 요청 처리
- `src/main/java/com/jjang051/petcity/memberfeature/dao/MemberFeatureMapper.java`
  - 독립 회원 기능용 DAO
- `src/main/java/com/jjang051/petcity/memberfeature/dto/MemberFeatureAccountDto.java`
  - 팀장 `MemberDto`를 변경하지 않기 위한 독립 DTO
- `src/main/java/com/jjang051/petcity/memberfeature/service/MemberFeatureService.java`
  - 탈퇴 대기 전환, 복구코드 해시 검증, 복구, 계정 찾기, 비밀번호 변경
- `src/main/resources/mapper/memberfeature/MemberFeatureMapper.xml`
  - 팀장 회원 매퍼와 분리된 독립 SQL 매퍼

구현 내용:

- 일반회원과 SNS 회원 모두 탈퇴 요청 즉시 `DELETE_PENDING`으로 전환
- 탈퇴 요청 후 3일이 지나면 영구 삭제 대상
- 탈퇴 사유와 영구 삭제 예정일 저장 및 표시
- 일반회원은 현재 비밀번호 확인 후 탈퇴
- SNS 회원은 별도 로컬 비밀번호 입력 없이 탈퇴
- 등록 이메일과 복구코드가 동일 회원 기준으로 모두 일치해야 즉시 복구
- 이메일 도메인이 아니라 DB `LOGIN_TYPE`으로 일반/SNS 회원 구분

### 3.2 복구코드 이메일 직접 발송

- `src/main/java/com/jjang051/petcity/mail/service/RecoveryCodeEmailService.java`
- `src/main/resources/templates/member/recovery-code-email.html`
- `src/main/resources/templates/member/recovery-code-email-complete.html`

최종 정책:

- 가입 시 저장된 이메일은 이미 확인된 주소로 간주
- 이메일 인증번호 발송·입력·검증 단계 제거
- 탈퇴 대기 계정과 가입 유형 확인 후 새 복구코드를 등록 이메일로 직접 발송
- 새 복구코드 발급 시 기존 복구코드는 사용할 수 없도록 교체
- 발송 후 로그인 화면이 아니라 이메일이 채워진 복구코드 입력 화면으로 이동
- 복구코드 원문은 DB에 저장하지 않고 해시만 저장

### 3.3 로그인·SNS·탈퇴 대기 계정 보호 필터

- `src/main/java/com/jjang051/petcity/memberfeature/security/MemberFeatureAdminDetailReasonFilter.java`
  - 팀장 관리자 상세 Controller/DTO를 수정하지 않고 탈퇴 사유 표시 보강
- `src/main/java/com/jjang051/petcity/memberfeature/security/MemberFeatureExistingSnsSignupFilter.java`
  - 기존 SNS 회원이 회원가입을 다시 시도하면 중복가입 안내
- `src/main/java/com/jjang051/petcity/memberfeature/security/MemberFeatureLoginFailureHandler.java`
  - 일반 로그인 실패와 탈퇴 대기·삭제 계정 실패 메시지 분리
- `src/main/java/com/jjang051/petcity/memberfeature/security/MemberFeatureOAuthAgreementBridgeFilter.java`
  - 별도 이메일 수집 동의 화면 없이 SNS가 제공한 이메일 사용
- `src/main/java/com/jjang051/petcity/memberfeature/security/MemberFeaturePendingAccountGuardFilter.java`
  - SNS 인증 성공 후에도 탈퇴 대기·삭제 계정의 서비스 진입 차단
- `src/main/java/com/jjang051/petcity/memberfeature/security/MemberFeatureSnsMyPageRedirectFilter.java`
  - SNS 회원을 독립 마이페이지로 연결

### 3.4 탈퇴·복구 보안 감사 이력

- `src/main/java/com/jjang051/petcity/member/dao/MemberSecurityAuditMapper.java`
- `src/main/java/com/jjang051/petcity/member/dto/MemberSecurityAuditDto.java`
- `src/main/java/com/jjang051/petcity/member/service/MemberSecurityAuditService.java`
- `src/main/java/com/jjang051/petcity/securityaudit/controller/MemberSecurityAuditAdminController.java`
- `src/main/resources/mapper/member/MemberSecurityAuditMapper.xml`
- `src/main/resources/templates/admin/recovery-request-list.html`

구현 내용:

- 탈퇴 요청, 복구 시도, 복구 성공·실패, 복구코드 재발급 이벤트 기록
- 회원 번호, 로그인 ID, 가입 유형, 결과, 사유, 접속 IP, 발생일 기록
- 복구코드와 인증번호 원문은 기록하지 않음
- 이력은 1년 보관 후 스케줄러로 자동 삭제
- 관리자 독립 URL: `/admin/recovery-requests`

### 3.5 마이페이지·아이디 찾기·비밀번호 기능

- `src/main/resources/templates/member/feature-mypage.html`
- `src/main/resources/templates/member/feature-change-password.html`
- `src/main/resources/templates/member/find-id.html`
- `src/main/resources/templates/member/find-password.html`
- `src/main/resources/templates/member/reset-password.html`
- `src/main/resources/templates/member/change-password.html`

구현 내용:

- 회원 상세 기준의 독립 마이페이지
- 아이디 찾기는 가입 이메일만 사용하고 아이디 전체 표시
- 비밀번호 찾기는 인증 요청 후 회원정보 입력값을 읽기 전용으로 유지
- 비밀번호 변경 성공 후 새 비밀번호로 로그인
- SNS 회원은 로컬 비밀번호 변경·탈퇴 비밀번호 입력 대상에서 제외

### 3.6 탈퇴·복구 및 계정 상태 화면

- `src/main/resources/templates/member/deleted-account.html`
- `src/main/resources/templates/member/feature-account-status.html`
- `src/main/resources/templates/member/feature-already-sns-member.html`
- `src/main/resources/templates/member/feature-withdraw.html`
- `src/main/resources/templates/member/recovery.html`
- `src/main/resources/templates/member/recovery-complete.html`
- `src/main/resources/templates/member/recovery-request-complete.html`
- `src/main/resources/templates/member/withdraw.html`
- `src/main/resources/templates/member/withdrawal-complete.html`

구현 내용:

- 관리자 삭제 계정의 삭제 사유 안내
- 탈퇴 대기 계정의 처리 상태 및 탈퇴 사유 안내
- 일반/SNS 유형별 등록 이메일과 복구코드 입력
- 탈퇴 접수 완료 시 복구코드 표시
- 안내 화면은 지정된 경우 10초 후 메인 화면 이동

### 3.7 자동 테스트

- `src/test/java/com/jjang051/petcity/memberfeature/security/MemberFeatureOAuthAgreementBridgeFilterTest.java`
- `src/test/java/com/jjang051/petcity/memberfeature/security/MemberFeaturePendingAccountGuardFilterTest.java`
- `src/test/java/com/jjang051/petcity/memberfeature/security/MemberFeatureSnsMyPageRedirectFilterTest.java`

검증 대상:

- SNS 제공 이메일 정책과 기존 SNS 회원 분기
- 탈퇴 대기 SNS 계정 로그인 차단
- SNS 회원 독립 마이페이지 이동

## 4. 추가·수정 SQL

### 20번 — 필수: 기존 회원 테이블 수정

`SQL/20_상각_07-19_20 sql 수정_APP_MEMBER 탈퇴 복구코드 컬럼.sql`

- `APP_MEMBER.RECOVERY_TOKEN_HASH` 추가
- 구버전 DB에 `DELETE_REASON`이 없을 때만 추가
- 복구코드 원문 대신 암호화 해시 저장
- 컬럼 존재 여부를 검사하므로 재실행 가능

### 21번 — 병원 이미지 적용 시 실행

`SQL/21_상각_07-19_20 sql 수정_병원 이미지 경로 매칭.sql`

- 병원 ID와 팀 제공 이미지 파일명을 연결해 `HOSPITAL.IMAGE_URL` 수정
- 이미지가 없는 병원은 다른 병원 사진이 노출되지 않도록 `NULL` 처리
- 회원 탈퇴·복구 기능과는 독립된 데이터 보정 SQL

### 22번 — 보안이력 기능 사용 시 필수

`SQL/22_상각_07-19_20 sql 추가_회원 탈퇴 복구 보안이력 1년보관.sql`

- `MEMBER_SECURITY_AUDIT` 신규 테이블
- `SEQ_MEMBER_SECURITY_AUDIT` 신규 시퀀스
- 회원·만료일 조회 인덱스 추가
- 회원 영구 삭제 후에도 이력을 유지하기 위해 회원 외래키를 설정하지 않음

### SQL 실행 순서

1. 20번: 회원 탈퇴·복구 기능 사용 전 필수
2. 21번: 병원 이미지 데이터가 필요한 환경에서만 적용
3. 22번: 탈퇴·복구 보안이력 화면 사용 전 필수

오늘 변경한 이메일 인증번호 제거 및 복구코드 직접 발송 기능은 기존 20번 컬럼을 사용하므로 추가 SQL이 필요하지 않습니다.

## 5. SQL 및 배포 문서

- `SQL/23_상각_07-19_20 Gradle 배포본 및 테스트 안내.md`
- `SQL/24_상각_07-19_20 SQL 적용 순서 및 기능 대응표.md`
- `SQL/25_상각_07-19_20 8081 기준 8080 추가 변경 파일 목록.md`
- `SQL/26_상각_07-19_20 최종 기능별 인수인계 문서.md`

## 6. 팀장 전달 요약

> 8081 팀장 원본의 Controller, DTO, DAO, Service, Mapper, HTML 및 0~19번 SQL은 수정하지 않았습니다. 8080에 `memberfeature`, `securityaudit`, 독립 필터·템플릿·매퍼와 20~22번 SQL을 추가해 회원 탈퇴, 3일 유예, 복구코드 이메일 직접 발송, 계정 복구, 마이페이지, 아이디 찾기, 비밀번호 변경, 보안이력 기능을 구현했습니다. 공통 경로 차이는 Git에서 제외할 로컬 `application.yml`뿐입니다.

