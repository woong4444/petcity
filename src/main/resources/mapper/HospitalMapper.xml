<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jjang051.petcity.hospital.dao.HospitalDao">

    <!--
        병원 목록 조회

        핵심 구조:
        HOSPITAL
        ↓
        HOSPITAL_ANIMAL
        ↓
        ANIMAL_TYPE

        동물 이름으로 비교하지 않고,
        HOSPITAL_ANIMAL.ANIMAL_ID 번호로 비교한다.
    -->
    <select id="findHospitalList"
            resultType="HospitalDto">

        SELECT DISTINCT
        H.HOSPITAL_ID AS hospitalId,
        H.NAME AS name,
        H.ADDRESS AS address,
        H.DETAIL_ADDRESS AS detailAddress,
        H.PHONE AS phone,
        H.DISTRICT AS district,
        H.LATITUDE AS latitude,
        H.LONGITUDE AS longitude,
        H.OPEN_TIME AS openTime,
        H.CLOSE_TIME AS closeTime,
        H.STATUS AS status,
        H.DESCRIPTION AS description,
        H.IMAGE_URL AS imageUrl,
        H.WEBSITE_URL AS websiteUrl,
        H.CREATED_AT AS createdAt,
        H.UPDATED_AT AS updatedAt

        FROM HOSPITAL H
        LEFT JOIN HOSPITAL_ANIMAL HA
        ON H.HOSPITAL_ID = HA.HOSPITAL_ID

        WHERE 1 = 1

        <!-- 동물은 하나만 선택 -->
        <if test="animalId != null">
            AND HA.ANIMAL_ID = #{animalId}
        </if>

        <!-- 지역은 여러 개 선택 가능 -->
        <if test="districts != null and districts.size() > 0">
            AND TRIM(H.DISTRICT) IN
            <foreach collection="districts" item="district" open="(" separator="," close=")">
                #{district}
            </foreach>
        </if>

        <!-- 검색어 -->
        <if test="keyword != null and keyword != ''">
            AND (
            H.NAME LIKE '%' || #{keyword} || '%'
            OR H.ADDRESS LIKE '%' || #{keyword} || '%'
            OR H.DETAIL_ADDRESS LIKE '%' || #{keyword} || '%'
            )
        </if>

        ORDER BY H.HOSPITAL_ID DESC
    </select>


    <select id="findDistrictList" resultType="string">
        SELECT DISTINCT
        TRIM(DISTRICT) AS district
        FROM HOSPITAL
        WHERE DISTRICT IS NOT NULL
        ORDER BY TRIM(DISTRICT)
    </select>


    <select id="findAnimalTypeList"
            resultType="AnimalTypeDto">

        SELECT
        ANIMAL_ID AS animalId,
        ANIMAL_NAME AS animalName,
        CATEGORY AS category
        FROM ANIMAL_TYPE
        ORDER BY ANIMAL_ID
    </select>

</mapper>