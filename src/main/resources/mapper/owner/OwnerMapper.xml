<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jjang051.petcity.owner.dao.OwnerMapper">

    <!-- 병원장 신청 -->
    <insert id="insertOwnerRequest">

        INSERT INTO HOSPITAL_OWNER_REQUEST
        (
        REQUEST_ID,
        MEMBER_ID,
        HOSPITAL_ID,
        BUSINESS_NUMBER,
        DOCUMENT_URL,
        STATUS
        )
        VALUES
        (
        SEQ_HOSPITAL_OWNER_REQUEST.NEXTVAL,
        #{memberId},
        #{hospitalId},
        #{businessNumber},
        #{documentUrl},
        'PENDING'
        )

    </insert>

</mapper>